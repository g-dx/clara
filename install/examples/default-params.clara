// Entry point
fn main() {
  greet()
  greet("Yo!")
  greet("Yo!", 10)
  greet("Yo!", 10, 100)
}

fn greet(msg: string = "Hello world!", len: int = msg.length, cubed: int = cube(len)) {
    printf("'%s' (%d characters), msg.length³ = %d\n", msg, len, cubed)
}

fn cube(x: int) int {
    return x * x * x
}