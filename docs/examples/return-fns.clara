// Entry point
fn main() {
  printf("double(inc(4)) + triple(double(2)) + inc(1) = %d\n", double(inc(4)) + triple(double(2)) + inc(1))
}

fn double(x : int) int {
    return x + x
}

fn triple(x: int) int {
    return x + double(x)
}

fn inc(x:int) int {
    return x + 1
}
